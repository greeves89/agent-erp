# AgentERP Community Edition — Docker Compose for End Users
#
# Quick start:
#   1. Download this file + .env.example: curl -LO agent-erp.dev/install/docker-compose.yml
#   2. Copy .env.example to .env and fill in your secrets
#   3. Run:  docker compose up -d
#   4. Open: http://localhost:3000
#
# Updates:  docker compose pull && docker compose up -d
# Logs:     docker compose logs -f
# Stop:     docker compose down
#
# Documentation: https://github.com/greeves89/AgentERP

services:
  postgres:
    image: pgvector/pgvector:pg16
    container_name: agent-erp-postgres
    environment:
      POSTGRES_DB: agent_erp
      POSTGRES_USER: agent_erp
      POSTGRES_PASSWORD: ${DB_PASSWORD:?DB_PASSWORD is required in .env}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U agent_erp"]
      interval: 5s
      timeout: 5s
      retries: 5
    restart: unless-stopped
    deploy:
      resources:
        limits:
          memory: 512M
    networks:
      - internal

  redis:
    image: redis:7-alpine
    container_name: agent-erp-redis
    command:
      - "redis-server"
      - "--appendonly"
      - "yes"
      - "--maxmemory"
      - "256mb"
      - "--maxmemory-policy"
      - "allkeys-lru"
      - "--requirepass"
      - "${REDIS_PASSWORD:?REDIS_PASSWORD is required in .env}"
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "-a", "${REDIS_PASSWORD}", "ping"]
      interval: 5s
      timeout: 5s
      retries: 5
    restart: unless-stopped
    deploy:
      resources:
        limits:
          memory: 384M
    networks:
      - internal
      - agent-network

  orchestrator:
    image: ghcr.io/greeves89/agent-erp-orchestrator:${AI_EMPLOYEE_VERSION:-latest}
    container_name: agent-erp-orchestrator
    pull_policy: always
    ports:
      - "${ORCHESTRATOR_PORT:-8000}:8000"
    environment:
      DATABASE_URL: "postgresql+asyncpg://agent_erp:${DB_PASSWORD}@postgres:5432/agent_erp"
      REDIS_URL: "redis://:${REDIS_PASSWORD}@redis:6379"
      REDIS_URL_INTERNAL: "redis://:${REDIS_PASSWORD}@redis:6379"
      # LLM Provider (set ONE of these)
      ANTHROPIC_API_KEY: ${ANTHROPIC_API_KEY:-}
      CLAUDE_CODE_OAUTH_TOKEN: ${CLAUDE_CODE_OAUTH_TOKEN:-}
      OPENAI_API_KEY: ${OPENAI_API_KEY:-}
      DEFAULT_MODEL: ${DEFAULT_MODEL:-claude-sonnet-4-6}
      MAX_TURNS: ${MAX_TURNS:-100}
      MAX_AGENTS: ${MAX_AGENTS:-10}
      # Security (REQUIRED — must be unique per installation!)
      ENCRYPTION_KEY: ${ENCRYPTION_KEY:?ENCRYPTION_KEY is required in .env (32+ random chars)}
      API_SECRET_KEY: ${API_SECRET_KEY:?API_SECRET_KEY is required in .env (32+ random chars)}
      # Optional
      TELEGRAM_BOT_TOKEN: ${TELEGRAM_BOT_TOKEN:-}
      TELEGRAM_CHAT_ID: ${TELEGRAM_CHAT_ID:-}
      CORS_ALLOW_ORIGIN: ${CORS_ALLOW_ORIGIN:-}
      OAUTH_REDIRECT_BASE_URL: ${OAUTH_REDIRECT_BASE_URL:-http://localhost:8000}
      # Agent image this orchestrator spawns
      AGENT_IMAGE: "ghcr.io/greeves89/agent-erp-agent:${AI_EMPLOYEE_VERSION:-latest}"
      AGENT_NETWORK: agent-erp-network
    volumes:
      # Docker socket is required so the orchestrator can spawn agent containers
      - /var/run/docker.sock:/var/run/docker.sock
      # Shared volume accessible by all agents for cross-agent file exchange
      - agent-erp-shared:/shared
    deploy:
      resources:
        limits:
          memory: 1G
          cpus: "2.0"
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8000/healthz', timeout=3)"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 30s
    networks:
      - internal
      - agent-network

  embedding-service:
    image: ghcr.io/greeves89/agent-erp-embedding:${AI_EMPLOYEE_VERSION:-latest}
    container_name: agent-erp-embedding
    pull_policy: always
    volumes:
      - embedding_models:/models
    restart: unless-stopped
    deploy:
      resources:
        limits:
          memory: 3G
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8001/healthz"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 120s
    networks:
      - internal

  frontend:
    image: ghcr.io/greeves89/agent-erp-frontend:${AI_EMPLOYEE_VERSION:-latest}
    container_name: agent-erp-frontend
    pull_policy: always
    ports:
      - "${FRONTEND_PORT:-3000}:3000"
    environment:
      NEXT_PUBLIC_API_URL: ${NEXT_PUBLIC_API_URL:-http://localhost:8000}
      NEXT_PUBLIC_WS_URL: ${NEXT_PUBLIC_WS_URL:-ws://localhost:8000}
    depends_on:
      orchestrator:
        condition: service_healthy
    restart: unless-stopped
    deploy:
      resources:
        limits:
          memory: 384M
          cpus: "1.0"
    networks:
      - internal

volumes:
  postgres_data:
    name: agent-erp-postgres-data
  redis_data:
    name: agent-erp-redis-data
  embedding_models:
    name: agent-erp-embedding-models
  agent-erp-shared:
    name: agent-erp-shared

networks:
  internal:
    driver: bridge
    name: agent-erp-internal
  agent-network:
    name: agent-erp-network
    driver: bridge
